# MIDI Fixtures

This folder stores deterministic fixture definitions used for verification:

- `fixture-specs.md` documents exact test scenarios.
- Runtime tests may synthesize `.mid` files from these specs to avoid binary drift.
